An awesome ReactJS project found on https://github.com/rbhatia46/React-Portfolio

Change the values in src/resumeData.js to suit your use-case.

Run the following commands to spin the up the local dev server on port 80.(http://localhost:80).
 - npm run build 
 - sudo PORT=80 serve -s build & 

